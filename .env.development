# Не задавайте wss://classtoria.ru — WS в dev идёт на /node/ws (vite → 127.0.0.1:3020/ws).
# Если API с прода: VITE_PROXY_TARGET=https://classtoria.ru (WebSocket всё равно на локальный Bun).

# DaData: только API-токен для подсказок (секретный ключ — на бэкенде).
VITE_DADATA_TOKEN=23de02cd2b41dbb9951f8991a41b808f4398ec6e
